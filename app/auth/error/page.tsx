import { Alert } from "flowbite-react";

export default function AuthErrorPage() {
  return (
    <div className="auth-error h-screen">
      <Alert color="failure">Error occurred in auth error</Alert>
    </div>
  );
}
